function falling_time(H)
    t = sqrt(2 * H / 9.81) .* H;
    numbers = 0.8 + (1.1 - 0.8) * rand(1, length(t));
    result = round(t.*numbers, 3);
    writematrix([t, result], 'falling_ball.xlsx');
end